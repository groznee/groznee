name: Update badges

on: workflow_dispatch

jobs:
  update-readme:
    name: Update Readme with badges
    runs-on: ubuntu-latest
    steps:
      - name: Badges - Readme
        uses: pemtajo/badge-readme@master
          CREDLY_USER: <ivan-milenkovic.d23915bc> # optional, but default will use the same from github
